[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "icu-problem-note-retrieval"
version = "0.1.0"
description = "Minimal deterministic problem-centric ICU note retrieval"
requires-python = ">=3.10"
authors = [{ name = "Haoze Yan" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

